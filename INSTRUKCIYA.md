# 📋 ПРОСТА ІНСТРУКЦІЯ ДЛЯ НАЛАШТУВАННЯ АВТОМАТИЧНОГО ДЕПЛОЮ

## ✅ ЩО ПОТРІБНО ЗРОБИТИ В HOSTINGER:

### Крок 1: Відкрийте налаштування деплою
- Зайдіть в панель Hostinger
- Знайдіть розділ "Автоматичне розгортання" або "Deployment"
- Знайдіть налаштування команди деплою

### Крок 2: Змініть команду деплою
**ЗАМІНІТЬ** стандартну команду `git pull` на:

```bash
bash deploy.sh
```

Або якщо не працює, використайте повний шлях:

```bash
cd /wp-content/themes/natura && bash deploy.sh
```

### Крок 3: Збережіть зміни
- Натисніть "Зберегти" або "Update"
- Готово! Тепер при кожному push в GitHub файли будуть автоматично оновлюватися на сайті

---

## 🔍 ЯК ПЕРЕВІРИТИ, ЩО ПРАЦЮЄ:

1. Зробіть тестовий коміт:
   ```bash
   git commit --allow-empty -m "TEST"
   git push
   ```

2. Перевірте на сайті - файли мають оновитися автоматично

3. Перевірте в GitHub:
   - Settings → Webhooks → Recent Deliveries
   - Має бути статус 200 (успішно)

---

## ❌ ЯКЩО НЕ ПРАЦЮЄ:

### Помилка про розбіжні гілки:
Виконайте на сервері через SSH:
```bash
cd /wp-content/themes/natura
git config pull.rebase false
git config pull.ff only
git fetch origin
git reset --hard origin/main
```

### Webhook не спрацьовує:
1. Перевірте URL webhook в GitHub (Settings → Webhooks)
2. URL має бути: `https://webhooks.hostinger.com/deploy/f8add29293caab24b51da9b07d93cf9b`
3. Перевірте, чи активний webhook (галочка "Active")

---

## 📞 ПІДСУМОК:

**ГОЛОВНЕ:** В Hostinger замініть команду `git pull` на `bash deploy.sh`

Це все! Після цього все буде працювати автоматично.


