# 📦 Інструкція: Деплой на два хостинги одночасно

## 🎯 Що робить скрипт

Автоматично пушить зміни в GitHub та деплоїть на два хостинги:
1. **Hostinger** (якщо налаштовано credentials)
2. **mx395217.ftp.tools** (новий хостинг)

---

## 📋 Варіанти деплою

### 1️⃣ `deploy-dual.sh` - Послідовний деплой (рекомендовано для стабільності)
Деплоїть на хостинги по черзі, один за одним.

### 2️⃣ `deploy-parallel.sh` - Паралельний деплой (швидше)
Деплоїть на обидва хостинги одночасно в фонових процесах.

---

## 🚀 Швидкий старт

### Послідовний деплой
```bash
./deploy-dual.sh "Опис змін"
```

### Паралельний деплой
```bash
./deploy-parallel.sh "Опис змін"
```

---

## ⚙️ Налаштування

### Для Hostinger (опціонально)

Якщо хочете деплоїть і на Hostinger, додайте змінні оточення:

```bash
export HOSTINGER_SFTP_HOST='ваш-хост.hostingersite.com'
export HOSTINGER_SFTP_USER='ваш-логін'
export HOSTINGER_SFTP_PASS='ваш-пароль'
```

Щоб зберігати ці налаштування постійно, додайте їх у `~/.bashrc` або `~/.zshrc`:

```bash
echo 'export HOSTINGER_SFTP_HOST="ваш-хост.hostingersite.com"' >> ~/.zshrc
echo 'export HOSTINGER_SFTP_USER="ваш-логін"' >> ~/.zshrc
echo 'export HOSTINGER_SFTP_PASS="ваш-пароль"' >> ~/.zshrc
source ~/.zshrc
```

### Для mx395217.ftp.tools (вже налаштовано)

Параметри вже прописані в скрипті:
- **Хост**: `mx395217.ftp.tools`
- **Логін**: `mx395217`
- **Пароль**: `Nature0211`
- **Шлях**: `/wp-content/themes/natura/`

---

## 📝 Приклади використання

### Деплой з описом коммиту
```bash
./deploy-dual.sh "Додано авторизацію через Google"
```

### Швидкий деплой без опису
```bash
./deploy-dual.sh
# Використає автоматичний опис "Auto deploy"
```

### Паралельний деплой
```bash
./deploy-parallel.sh "Виправлено помилки авторизації"
```

---

## 🔍 Що синхронізується

✅ **Включено:**
- Всі PHP файли
- CSS та JavaScript
- Зображення та інші assets
- Структура директорій

❌ **Виключено:**
- `.git` папка
- `.DS_Store` файли
- `.github` папка
- Скрипти деплою (`deploy*.sh`)
- Документація (`*.md`, `*.txt`)

---

## 🐛 Рішення проблем

### Помилка: "lftp не установлен"

**macOS:**
```bash
brew install lftp
```

**Linux:**
```bash
sudo apt-get update && sudo apt-get install -y lftp
```

### Помилка: "Permission denied"

Перевірте права на виконання:
```bash
chmod +x deploy-dual.sh deploy-parallel.sh
```

### Помилка підключення до SFTP

1. Перевірте правильність хоста, логіну та пароля
2. Перевірте, чи доступний хостинг через інтернет
3. Перевірте шлях до теми на сервері (зазвичай `/wp-content/themes/natura/`)

### Помилка: "cd: no such file or directory"

Переконайтеся, що шлях до теми правильний. Можливо потрібно змінити `NEW_PATH` в скрипті.

---

## 📊 Відмінності між скриптами

| Функція | deploy-dual.sh | deploy-parallel.sh |
|---------|----------------|-------------------|
| Швидкість | Повільніше (послідовно) | Швидше (паралельно) |
| Стабільність | Вища | Може бути більше помилок |
| Логи | В реальному часі | Зберігаються в /tmp |
| Рекомендовано | ✅ Для продакшену | ⚡ Для швидкого тестування |

---

## 🔐 Безпека

⚠️ **Важливо:**
- Не комітьте файли з паролями в Git
- Використовуйте змінні оточення для чутливих даних
- Перевірте `.gitignore` перед деплоєм

---

## 💡 Поради

1. **Завжди тестуйте на staging перед продакшеном**
2. **Перевіряйте логи при помилках** (особливо для `deploy-parallel.sh`)
3. **Зберігайте резервні копії перед великими деплоями**
4. **Використовуйте `deploy-dual.sh` для продакшену** (більш стабільний)

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте логи помилок
2. Перевірте налаштування SFTP credentials
3. Перевірте доступність хостингів через інтернет
