# 🚀 Швидка інструкція: Автоматичний деплой на обидва хостинги

## ✅ Що вже готово

- ✅ GitHub Webhook для Hostinger (вже налаштовано)
- ✅ GitHub Actions workflow для mx395217.ftp.tools (створено)

## 🔧 Що потрібно зробити ОДИН РАЗ

### Крок 1: Додати Secrets в GitHub

1. Перейдіть в ваш репозиторій на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. Натисніть **"New repository secret"**

Додайте 3 Secrets:

| Name | Value | Кнопка |
|------|-------|--------|
| `MX395217_SFTP_HOST` | `mx395217.ftp.tools` | Add secret |
| `MX395217_SFTP_USER` | `mx395217` | Add secret |
| `MX395217_SFTP_PASS` | `Nature0211` | Add secret |

### Крок 2: Закомітьте workflow файли

```bash
git add .github/workflows/
git commit -m "Add GitHub Actions workflow for dual deployment"
git push
```

### Крок 3: Перевірка

Після push перевірте:
1. Перейдіть в **Actions** (верхнє меню GitHub)
2. Побачите workflow "Deploy to both hostings"
3. Він має виконатися успішно ✅

## 🎉 Готово!

Тепер при кожному push:
- ✅ **Hostinger** оновлюється автоматично через webhook
- ✅ **mx395217.ftp.tools** оновлюється автоматично через GitHub Actions

## 📝 Як працювати

Просто робіть коміти і push як завжди:

```bash
git add .
git commit -m "Ваші зміни"
git push
```

Обидва хостинги оновляться автоматично! 🎊

## 🔍 Перевірка деплою

Після кожного push:
1. Перейдіть в **Actions** на GitHub
2. Відкрийте останній workflow run
3. Перевірте, що всі кроки зелені ✅

## 🐛 Якщо щось не працює

1. **Перевірте Secrets** - чи всі додані правильно
2. **Перевірте Actions** - чи є помилки в логах
3. **Перевірте webhook** - Settings → Webhooks → перевірте останній delivery

Детальна інструкція: `.github/workflows/README.md`
